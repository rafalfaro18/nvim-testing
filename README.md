# My Neovim 0.12.0 config from scratch

Made from scratch minimal, used with a different value for ``NVIM_APPNAME`` as a secondary neovim config separate from the default one.

## Instructions

```sh
NVIM_APPNAME=nvim-testing nvim .
```
